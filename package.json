{
  "name": "file-dialog",
  "version": "0.0.0",
  "description": "Programable calling file dialog",
  "main": "index.js",
  "repository": "https://github.com/TomokiMiyauci/file-dialog",
  "license": "MIT",
  "private": false,
  "scripts": {
    "lint": "eslint --ext .ts --ignore-path .gitignore ."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.7.0",
    "@typescript-eslint/parser": "^4.7.0",
    "commitizen": "^4.2.2",
    "eslint": "^7.13.0",
    "husky": "^4.3.0",
    "lint-staged": "^10.5.1",
    "prettier": "^2.1.2",
    "typescript": "^4.0.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.ts": "eslint --fix",
    "*": "prettier -w -u"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
